# Machine-readable active-sprint snapshot for Code-Gen-AI.
# Canonical narrative version: docs/engineering/ENGINEERING_MASTER_PLAN.md (section 6).
#
# No sprint cadence, sprint numbering, or start/end dates were found anywhere in this repo
# (no sprint board, no dated backlog entries). The `window` fields below are marked
# needs-audit rather than invented — populate them from the team's actual tracker.

schema_version: 1
generated_by: "engineering-baseline (2026-07-05)"

sprint:
  id: needs-audit
  window:
    start: needs-audit
    end: needs-audit
  goal: >
    Close out M3.2 Component Adaptation and begin M3.3 Manifest Persistence. M3.3 is now
    implemented (SPR-3 through SPR-6 done) and merged to main (PR #23). M4 Verification
    Engine kickoff planning (SPR-7), slice 1 (SPR-8), slice 2 (SPR-9), slice 3 (bounded
    repair loop, SPR-10), and slice 4 (timing + scope decisions, SPR-11) are all done and
    merged to main (PR #27, PR #28). SPR-12 (real end-to-end verification run) is further
    narrowed but still not fully closed: this session ran the real server against a real
    local Postgres instance and drove a real job through the actual HTTP API, directly
    observing BUILD fail with a genuine 401 Bad credentials from the real GitHub API before
    ever reaching QA; separately, the verification+repair mechanics were proven against
    genuinely real npm/eslint execution and real qa_runs persistence, with
    decideRepairAction() fed real (not fixture) VerificationResults. A follow-up attempt
    pointed at Railway (where real credentials are understood to exist) but found this
    session has no technical channel to Railway at all (no CLI, no API token, no MCP
    connector, no documented deployment URL) — a harder, environmental blocker rather than a
    credentials-are-bad one. M4 remains in_progress, not moved toward M5. See RISK-18's
    remediation_checklist and railway_access_prerequisites in project/risks.yaml. M3.2's
    ComponentHarvester.jsx wiring audit (SPR-1) and candidate-scoring scope decision (SPR-2)
    remain open.
  milestones_in_focus: [M3.2, M3.3, M4]

  items:
    - id: SPR-1
      title: "Audit ComponentHarvester.jsx wiring to /api/registry and /api/adapt"
      milestone: M3.2
      status: todo
      evidence_so_far: ["src/pages/ComponentHarvester.jsx", "src/pages/ComponentHarvester.test.jsx"]
      note: "This baseline confirmed the page and a test file exist but did not trace live wiring end-to-end."

    - id: SPR-2
      title: "Decide candidate-scoring scope for M3.2 vs. deferred"
      milestone: M3.2
      status: todo
      reference: "docs/06-component-harvester.md:49-65"

    - id: SPR-3
      title: "Answer open question: data-model target for M3.3+ (lean jobs/plans schema vs. full ERD in docs/10-data-model.md)"
      milestone: M3.3
      status: done
      evidence: ["adr/0007-manifest-persistence-data-model.md"]
      note: >
        Decided: build M3.3 on the existing lean jobs/plans/qa_runs schema (additive
        component_manifests-style table); defer the full ERD until after M5. ADR-0007 is now
        Accepted (2026-07-05).

    - id: SPR-4
      title: "Design and land manifest persistence table(s)"
      milestone: M3.3
      status: done
      evidence: ["server/src/db/migrations/005_component_manifests.sql"]
      note: >
        component_manifests table added, FK'd to jobs(id)/plans(id) (nullable, ON DELETE SET
        NULL). Type-checked and included in `npm run build`'s dist/db/migrations output; not
        executed against a live Postgres in this environment (no test touches db/client
        directly, same as the existing registry_components table).

    - id: SPR-5
      title: "Implement manifest-generation service and wire into /api/adapt"
      milestone: M3.3
      status: done
      evidence: ["server/src/services/harvester/manifest-store.ts", "server/src/routes/adapt.ts"]
      note: >
        buildManifest()/validateManifest() mint schema-compatible hc_*/plan_* IDs and validate
        every manifest via a Zod schema mirroring contracts/component-manifest.schema.json;
        /api/adapt's /component and /batch endpoints now build and persist one manifest per
        adaptation and return it in the response.

    - id: SPR-6
      title: "Add tests for manifest generation/persistence mirroring adapt.test.ts / registry.test.ts patterns"
      milestone: M3.3
      status: done
      evidence: ["server/src/services/harvester/manifest-store.test.ts", "server/src/routes/adapt.test.ts"]
      note: "75 server tests passing (up from 61 at baseline); covers manifest building, schema validation (valid + rejected cases), ID-prefix format, and a manifestToRow/rowToManifest round-trip."

    - id: SPR-7
      title: "M4 Verification Engine kickoff plan"
      milestone: M4
      status: done
      evidence: ["docs/engineering/M4_VERIFICATION_ENGINE_PLAN.md"]
      note: >
        Planning-only, on branch feature/m4-verification-engine-plan (no product code changed).
        Scopes 4 incremental slices (sandbox spike + single check → all four checks → bounded
        repair loop → observability polish). Key finding: no local checkout/sandbox execution
        capability exists today (server/src/services/github/index.ts:1-7) — slice 1 must
        establish that before any real check can run. qa_runs needs no schema change for
        slices 1-2.

    - id: SPR-8
      title: "Implement M4 slice 1: sandbox spike + single-check execution"
      milestone: M4
      status: done
      evidence:
        - "server/src/services/verification/workspace.ts"
        - "server/src/services/verification/checks.ts"
        - "server/src/services/verification/verify.ts"
        - "server/src/services/orchestrator/index.ts"
        - "server/src/services/verification/workspace.test.ts"
        - "server/src/services/verification/checks.test.ts"
        - "server/src/services/verification/verify.test.ts"
      note: >
        Real lint check + qa_runs persistence, on feature/m4-verification-slice-1. Workspace
        materialization reuses the existing github.getRepoFiles fetcher (no new git-clone/tar
        dependency). Command execution via injectable execFile, never a shell string — no
        shell-injection surface. No repair loop, no gating (job still proceeds to preview
        regardless of lint outcome), build/typecheck/tests still NULL — all per slice 1's
        deliberately narrow scope. 26 new tests (101/101 server tests passing, up from 75).
        Not yet verified against a live Postgres/GitHub token/Railway environment (none
        available in this session) — see docs/engineering/M4_VERIFICATION_ENGINE_PLAN.md
        section 11.

    - id: SPR-9
      title: "Implement M4 slice 2: all four checks + gating + GET /api/jobs/:id/qa"
      milestone: M4
      status: done
      evidence:
        - "server/src/services/verification/verify.ts"
        - "server/src/services/orchestrator/index.ts"
        - "server/src/routes/jobs.ts"
        - "server/src/services/verification/verify.test.ts"
        - "server/src/routes/jobs.test.ts"
      note: >
        All four checks (lint/build/typecheck/test) now run — each independently detected via
        package.json and skippable — and persist one full qa_runs row per job (no migration
        needed, confirming the plan's section 3 prediction). The orchestrator's QA step now
        throws on a failed/errored check, reusing the existing runJobPipeline ->
        createJob().catch() -> status='failed' convention rather than adding a new manual
        status-update path; a skipped check never blocks progression. New GET
        /api/jobs/:id/qa route (server/src/routes/jobs.ts) returns a job's qa_runs history,
        most recent first. 7 new/updated tests (108/108 server tests passing, up from 101).
        Still no repair loop (slice 3) and still not verified against a live
        Postgres/GitHub token/Railway environment (none available in this session).

    - id: SPR-10
      title: "Implement M4 slice 3: bounded repair loop"
      milestone: M4
      status: done
      evidence:
        - "server/src/services/orchestrator/index.ts"
        - "server/src/services/orchestrator/index.test.ts"
      note: >
        decideRepairAction() (pure, exported) decides proceed/repair/fail from a verification
        result + qa_runs row count: a failed check repairs up to 2 attempts; an errored check
        never repairs (fails immediately); a skipped check never blocks. generateAndCommitFiles()
        is shared by BUILD and REPAIR so the repair attempt reuses the same provider-call-then-
        commit flow, with a dedicated REPAIR_STAGE_INSTRUCTIONS prompt (plan + files generated
        so far + exact failing output, asking for a minimal fix). No qa_runs migration. 10 new
        tests against the pure decision function (118/118 server tests passing, up from 108).
        Repair mechanics are unit-tested; NOT yet demonstrated fixing a real failure with a real
        provider — no live Postgres/GitHub token/provider API key available in this session.

    - id: SPR-11
      title: "Implement M4 slice 4: observability polish + scope decision on remaining checks"
      milestone: M4
      status: done
      evidence:
        - "adr/0008-m4-verification-scope-boundaries.md"
        - "server/src/services/verification/checks.ts"
        - "server/src/services/verification/verify.ts"
        - "server/src/services/verification/checks.test.ts"
        - "server/src/services/verification/verify.test.ts"
      note: >
        ADR-0008 (Accepted) decides all four open scope questions: preview-error-triggered
        repair deferred (no capture mechanism exists yet, different pipeline stage), accessibility
        smoke checks deferred (needs a live running instance, not a package.json script), license
        checks deferred/out-of-scope entirely (already owned by M3.3/RISK-3/RISK-8), structured
        timing added now. CheckResult gained an in-memory-only durationMs field (null when a
        check never actually ran; a real measured value for every outcome that did, including
        errored-via-timeout). No qa_runs migration, per the ADR. 119/119 server tests passing
        (up from 118 after slice 3).

    - id: SPR-12
      title: "Manually verify M4 slices 1-3 end-to-end against a real job"
      milestone: M4
      status: in_progress
      evidence:
        - "server/src/services/verification/verify.ts (persistQaRun, listQaRuns, runVerification)"
        - "server/src/services/orchestrator/index.ts (decideRepairAction)"
        - "server/src/routes/generations.ts (POST /api/generations, real job creation)"
      note: >
        Partially done (M4 slice 4, 2026-07-06): with a manually-started local Postgres
        instance, all 5 migrations were applied for the first time in this project's history,
        and persistQaRun/listQaRuns were exercised against a genuine database (not injected
        fakes) via a disposable script — confirmed correct round-trip behavior and
        most-recent-first ordering.

        Further narrowed (2026-07-06, feature/m4-e2e-verification-proof): the real Express
        server was started against a real local Postgres instance and driven through its actual
        HTTP API. POST /api/generations created a real job; PLAN ran for real (mock provider);
        BUILD then failed with a genuine 401 Bad credentials from the real GitHub REST API at
        the branch-creation call — captured directly from server logs and the job's persisted
        error_message (status='failed'), confirmed via psql that zero qa_runs rows exist for
        that job (it never reached QA). Separately, a real npm install + real npm run lint
        against a demo repo with a genuine ESLint no-unused-vars violation exercised
        runVerification()/decideRepairAction() for real: a real failure was detected and
        persisted to qa_runs; decideRepairAction(), fed that real VerificationResult, correctly
        chose to repair; after a fix (manual edit, standing in for an LLM patch — no provider
        key available), a real re-run passed, persisted a second real qa_runs row, and
        decideRepairAction() correctly chose to proceed — independently confirmed via direct SQL
        query. Still blocked: this session's GITHUB_TOKEN produces a genuine 401 from the real
        server's own Octokit client (not just a raw curl 403 as in the prior session), and no
        OPENAI_API_KEY/ANTHROPIC_API_KEY is present. This is still the single biggest unverified
        assumption across M4, now narrowed to precisely two things: a real GitHub PAT (for
        branch/commit operations) and a real provider API key (for genuine fix content) — see
        RISK-18's remediation_checklist in project/risks.yaml.

        Railway attempt (2026-07-06, feature/m4-final-railway-e2e-proof): pointed at Railway
        specifically, since a real GitHub token and provider key are understood to be configured
        there. Found a harder blocker: this session has no technical channel to Railway at all —
        no CLI, no API token, no MCP connector (confirmed via ListConnectors), no documented
        deployment URL anywhere in the repo, no GitHub Actions/Deployments-API path to discover
        one. Still in_progress, not done — this remains the single biggest open item for M4, now
        additionally blocked on Railway access itself, not just on which credentials to use once
        connected. See RISK-18's railway_access_prerequisites in project/risks.yaml for what a
        future session or human operator needs to actually close this.

  out_of_sprint_but_flagged:
    - "Plan-schema validation and provider_calls logging — cross-cutting, not scheduled to a milestone."
