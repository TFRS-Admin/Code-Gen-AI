# Machine-readable risk register for Code-Gen-AI.
# Canonical narrative version: docs/engineering/ENGINEERING_MASTER_PLAN.md (section 8).
# Carries forward docs/14-milestones.md's risk register, plus risks confirmed by repo
# evidence during the 2026-07-05 engineering baseline audit.

schema_version: 1
generated_by: "engineering-baseline (2026-07-05)"

risks:
  # ── Carried over from docs/14-milestones.md:83-90 ──
  - id: RISK-1
    title: "Invalid JSON plans"
    source: "docs/14-milestones.md"
    probability: medium
    impact: high
    mitigation: "Structured outputs, retries, schema repair"
    status: open
    repo_evidence: >
      Confirmed still open: no code path validates the provider's plan output against
      contracts/plan.schema.json (see server/src/services/orchestrator/index.ts).

  - id: RISK-2
    title: "Preview dependency mismatch"
    source: "docs/14-milestones.md"
    probability: medium
    impact: medium
    mitigation: "Dependency allowlist, WebContainers phase 2"
    status: mitigated
    repo_evidence: >
      WebContainers phase 2 shipped (ADR-0006), superseding the original Sandpack-only plan.
      Dependency allowlist for harvested components exists in docs/06-component-harvester.md
      but is not yet enforced in code (see RISK-8).

  - id: RISK-3
    title: "License ambiguity"
    source: "docs/14-milestones.md"
    probability: medium
    impact: medium
    mitigation: "Manifest license field, review"
    status: partially_mitigated
    repo_evidence: >
      M3.3 implemented (adr/0007-manifest-persistence-data-model.md, Accepted;
      server/src/services/harvester/manifest-store.ts): every adapted component now produces
      a manifest with a required, schema-validated `license` field, persisted to
      component_manifests. Not fully mitigated: when no registry component is matched
      (componentMetadata is null), license is recorded as the literal string "unknown" rather
      than a reviewed value — there is still no human review step gating on that field.

  - id: RISK-4
    title: "TFRS drift"
    source: "docs/14-milestones.md"
    probability: high
    impact: medium
    mitigation: "Design adapter and checklist"
    status: partially_mitigated
    repo_evidence: >
      TFRS class adapter exists and is unit-tested
      (server/src/services/harvester/tfrs-adapter.ts, tfrs-adapter.test.ts). No automated
      design checklist / accessibility smoke test found (docs/15-gathering-results.md targets
      an 85%+ accessibility smoke pass rate with no implementation evidence).

  - id: RISK-5
    title: "Lifecycle bypass"
    source: "docs/14-milestones.md"
    probability: medium
    impact: high
    mitigation: "AI rules and CI checks"
    status: partially_mitigated
    repo_evidence: >
      AI rules exist and are thorough (CLAUDE.md, .cursor/rules, .github/copilot-instructions.md).
      No CI checks were found enforcing them (no .github/workflows found in this repo at
      baseline time) — mitigation is currently doc-only, not automated.

  - id: RISK-6
    title: "Scope creep into deployment"
    source: "docs/14-milestones.md"
    probability: medium
    impact: high
    mitigation: "Keep deployment out of MVP"
    status: open
    repo_evidence: >
      Deployment tooling (Railway configs, Procfile, Caddyfile, nixpacks.toml) already exists
      at both root and server/ — deployment is in scope in practice, which is fine for an
      operating product but is a scope difference from the original "keep deployment out of
      MVP" framing. Not necessarily a problem; flagged so it's an explicit decision rather than
      silent drift.

  # ── New risks confirmed during the 2026-07-05 baseline audit ──
  - id: RISK-7
    title: "QA/verification step is a non-functional placeholder"
    source: "engineering-baseline-audit"
    probability: high
    impact: high
    mitigation: "Implement M4 (real lint/build/typecheck/test execution + qa_runs persistence + repair loop)"
    status: open
    repo_evidence: >
      server/src/services/orchestrator/index.ts:357-367 — the 'qa' phase only appends log
      lines and contains an explicit TODO(M3) comment; qa_runs table has zero writers found
      in server/src. Jobs can reach 'review' status with no real verification signal.

  - id: RISK-8
    title: "No enforcement of the component dependency allowlist/disallow list at build time"
    source: "engineering-baseline-audit"
    probability: medium
    impact: medium
    mitigation: "Add a dependency-check step to the adapt/manifest pipeline (M3.3/M4)"
    status: open
    repo_evidence: >
      docs/06-component-harvester.md:118-145 defines allowed/review/disallowed dependency
      lists, but server/src/services/harvester has no code that checks a component's
      dependencies against these lists.

  - id: RISK-9
    title: "Documentation drift between docs/08, docs/09, docs/10 and shipped architecture"
    source: "engineering-baseline-audit"
    probability: high
    impact: medium
    mitigation: "Revise the three docs to match shipped architecture, or record deltas in a new ADR"
    status: open
    repo_evidence: >
      See docs/engineering/ENGINEERING_MASTER_PLAN.md section 2.2 items 1-3 for full detail
      (preview tech substitution, leaner data model, job-centric API vs. generation-resource API).

  - id: RISK-10
    title: "Pre-existing TypeScript errors block using typecheck as a merge gate"
    source: "engineering-baseline-audit"
    probability: high
    impact: low
    mitigation: "Triage and fix src/pages/Projects.jsx and src/pages/Settings.jsx forwardRef/prop typing errors"
    status: open
    repo_evidence: >
      `npm run typecheck` at repo root produced 165 pre-existing errors, concentrated in
      src/pages/Projects.jsx and src/pages/Settings.jsx, at baseline time (before any changes
      in this task). ESLint (`npm run lint`) is clean, so this is tsc-specific.

  - id: RISK-11
    title: "Branch-strategy mismatch: docs say develop, repo only has main"
    source: "engineering-baseline-audit"
    probability: medium
    impact: medium
    mitigation: "Either create a develop branch or update CLAUDE.md/docs/04-repository-contracts.md to say main"
    status: open
    repo_evidence: >
      `git branch -a` at baseline time showed only `main` and short-lived `claude/*` branches
      on the remote — no `develop` branch exists, despite CLAUDE.md and
      docs/04-repository-contracts.md:116 specifying a `develop -> feature/*` workflow.

  # ── Risks from the M3.3 manifest-persistence ADR audit (2026-07-05) — resolved by implementation ──
  - id: RISK-12
    title: "Manifest schema ID patterns do not match the running schema's raw UUID IDs"
    source: "adr/0007-manifest-persistence-data-model.md"
    probability: high
    impact: medium
    mitigation: >
      Mint synthetic prefixed IDs (hc_<uuid> for manifestId, plan_<uuid-or-adhoc> for
      generationPlanId) at manifest-build time, per
      adr/0007-manifest-persistence-data-model.md's Decision section, rather than changing how
      jobs/plans primary keys work internally.
    status: resolved
    repo_evidence: >
      Implemented in server/src/services/harvester/manifest-store.ts (mintManifestId,
      mintGenerationPlanId), enforced by ComponentManifestSchema's regex validation on every
      buildManifest() call, and covered by
      server/src/services/harvester/manifest-store.test.ts's ID-prefix-format test.

  - id: RISK-13
    title: "ADR-0007 is Proposed, not yet Accepted — migration should not start on it as-is"
    source: "adr/0007-manifest-persistence-data-model.md"
    probability: medium
    impact: medium
    mitigation: "Get explicit owner (Blair) sign-off to flip ADR-0007's Status to Accepted before any M3.3 schema migration is written, per the task's quality bar (\"No schema migration until ADR is written and accepted\")."
    status: resolved
    repo_evidence: >
      adr/0007-manifest-persistence-data-model.md's Status is now "Accepted" (2026-07-05,
      accepted by product owner / technical lead for M3.3 implementation), with an explicit
      note that the full-ERD alternative is not being reopened. The M3.3 migration
      (005_component_manifests.sql) was written after this acceptance.

  # ── New risk from the M3.3 implementation (2026-07-05) ──
  - id: RISK-14
    title: "Manifest `score` field is a fixed placeholder, not a computed candidate score"
    source: "M3.3 implementation"
    probability: high
    impact: low
    mitigation: >
      Implement candidate scoring (TICKET-029, docs/06-component-harvester.md:49-65's weighted
      0-100 model) and replace the two fixed constants
      (REGISTRY_MATCH_PLACEHOLDER_SCORE / UNMATCHED_CUSTOM_PLACEHOLDER_SCORE) in
      server/src/services/harvester/manifest-store.ts with a real score. Does not block M3.3's
      exit criteria (docs/14-milestones.md:41-48 only requires "Manifests validate", not
      "manifests are scored"; EMP Open Question 4 explicitly allowed shipping with score
      fixed/omitted).
    status: open
    repo_evidence: >
      server/src/services/harvester/manifest-store.ts defines
      REGISTRY_MATCH_PLACEHOLDER_SCORE = 100 and UNMATCHED_CUSTOM_PLACEHOLDER_SCORE = 0 with an
      explicit comment that these are not a computed signal — TICKET-029 (candidate scoring)
      remains status: todo in project/backlog.yaml.
